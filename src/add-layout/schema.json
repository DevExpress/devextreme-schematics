{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsDevextremeAddLayout",
  "title": "Add DevExtreme Layout to the Angular application.",
  "type": "object",
  "properties": {
    "layout": {
      "type": "string",
      "description": "The type of DevExtreme layout to include",
      "default": "side-nav-outer-toolbar"
    },
    "project": {
      "type": "string",
      "description": "The name of project to modify"
    },
    "empty": {
      "type": "boolean",
      "description": "Skip sample views generation",
      "default": false
    },
    "overrideAppComponent": {
      "type": "boolean",
      "description": "Specifies if the app component will be override",
      "default": false
    }
  }
}
